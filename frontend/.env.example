# Copy this file to .env and fill in the values.
# This file IS committed to source control — do NOT put real secrets here.

# Local development backend (no trailing slash).
# Production uses the value in .env.production or the Vercel environment.
VITE_API_URL=http://localhost:5000
