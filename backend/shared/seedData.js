/**
 * Shared seed data – CommonJS wrapper.
 * The single source of truth lives in /shared/seedData.json.
 * Backend scripts and routes require() this module.
 */
module.exports = require('../../shared/seedData.json');
